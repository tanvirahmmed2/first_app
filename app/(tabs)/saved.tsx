import { View, Text } from 'react-native'
import React from 'react'

const saved = () => {
  return (
    <View className='w-full flex items-center justify-center h-full'>
      <Text className='text-4xl font-semibold text-sky-400'>Saved</Text>
    </View>
  )
}

export default saved